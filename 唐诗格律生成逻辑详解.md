# 唐诗格律自动生成系统 —— 详细逻辑文档

## 一、系统概览

本系统基于 **GLM-10B-Chinese** 大语言模型，结合**受限束搜索（Constrained Beam Search）**与**物理格律规则过滤**，实现中国古典格律诗（五言/七言绝句/律诗）的自动生成。

核心思想是：**在自回归生成的每一步 token 采样时，实时调用格律校验器过滤违规候选，确保最终输出的每个字都符合唐诗格律规范。**

---

## 二、诗歌格式体系

### 2.1 格式分类

唐诗格律诗分为四大基本类型：

| 格式 | 每句字数 | 全诗句数 | 示例 |
|------|---------|---------|------|
| 五言绝句（五绝） | 5 | 4 | 床前明月光，疑是地上霜。举头望明月，低头思故乡。 |
| 五言律诗（五律） | 5 | 8 | （8句五言） |
| 七言绝句（七绝） | 7 | 4 | 日照香炉生紫烟，遥看瀑布挂前川。飞流直下三千尺，疑是银河落九天。 |
| 七言律诗（七律） | 7 | 8 | （8句七言） |

### 2.2 平仄格式

每句诗还有**平起**和**仄起**两种开头方式：

- **平起**：第二字为平声（rhy = 0）
- **仄起**：第二字为仄声（rhy = 1）

对于律诗（8句），各句平仄有严格的交替规律：
- 第 0、3、4、7 句：使用原始平仄模式（rhy）
- 第 1、2、5、6 句：使用反向平仄模式（1-rhy）

### 2.3 押韵规则

- **偶数句（第 1、3、5、7 句）必须押韵**（need_yy = 1）
- **奇数句（第 0、2、4、6 句）不需押韵**（need_yy = 0）
- **第一句（第 0 句）特殊处理**：如果末尾字是平声，则首句也需入韵
- **一韵到底**：全诗所有押韵句的韵脚必须在同一韵部（基于平水韵）

---

## 三、核心数据结构

### 3.1 平水韵词典（pingshui.txt）

系统加载 `pingshui.txt` 建立三个核心数据结构：

```python
worddict   # {汉字: [韵部编号列表]}    — 多音字可能有多个韵部
shengdict  # {汉字: [声调列表]}        — 0=平声, 1=仄声（上/去/入）
allbu      # [[韵部汉字列表], ...]     — 按韵部分组的汉字
allsb      # [[所有平声字], [所有仄声字]]  — 按声调分组的汉字
```

**关键设计 —— 多音字妥协**：
```python
# 多音字检测: len(shengdict[char]) > 1 或 len(worddict[char]) > 1
# 当字符有多个读音时，直接放行，不做平仄/押韵限制
if len(pz1) == 1:  # 只有单一读音才做平仄检查
    if rhy != pz1[0]:
        return -1000  # 平仄违规
```

### 3.2 校验器参数数组

`poem_verifier()` 通过一个参数数组传递所有校验状态：

| 索引 | 参数名 | 含义 | 取值含义 |
|------|--------|------|---------|
| 0 | tokenizer | 分词器 | GLM tokenizer |
| 1 | wdic | 韵部字典 | worddict |
| 2 | shengdict | 平仄字典 | shengdict |
| 3 | rhy | 当前句平仄模式 | 0=平起, 1=仄起, 2=未确定 |
| 4 | endrhy | 句尾平仄模式 | 0=平收（押韵句）, 1=仄收（非押韵句）, 2=未确定 |
| 5 | min_length | 最短字数 | 5（五言）或 7（七言） |
| 6 | max_length | 最长字数 | 5（五言）或 7（七言） |
| 7 | end_tokens | 句尾标点 | ["。","."] 或 ["，",","] |
| 8 | yayun | 已积累的押韵字 | 所有已生成的押韵句的末尾字 |

### 3.3 诗句编号与交替规律

8句律诗句子的编号和对应规则：

```
句号:   0       1       2       3       4       5       6       7
标点:   ，      。      ，      。      ，      。      ，      。
押韵:   否      是      否      是      否      是      否      是
平仄:   rhy    1-rhy   1-rhy   rhy     rhy    1-rhy   1-rhy   rhy
句尾:   仄收    平收    仄收    平收    仄收    平收    仄收    平收
```

**绝句（4句）使用相同的规律，只取前 4 句。**

---

## 四、格律校验规则详解

### 4.1 字数限制

```python
# poem_verifier.py:268-284
if len(st) < min_length:      # 句子过短 → 拒绝
    return -1000
if len(st) == 6:              # 六字绝对禁止 → 拒绝
    return -1000
if len(st) > max_length:      # 句子过长 → 拒绝
    return -1000
```

强制 5 或 7 字，**6 字是被绝对禁止的**（这是唐诗格律的基本要求）。

### 4.2 禁字过滤

```python
# poem_verifier.py:317-320
if st[i] in ['的', '些', '么', '了']:
    return -1000  # 出现现代白话虚词 → 拒绝
```

禁止现代汉语白话虚词，确保文言的古典风格。

### 4.3 重字与重复检测

系统有多层重复检测机制：

```python
# 1. 三字连续匹配 → 拒绝
if decode_token[i:i+3] in prev:
    return -1000

# 2. 二字连续匹配 → 拒绝
if st[i:i+2] in prev or st[i:i+2] in st[:i]:
    return -1000

# 3. 单字与上文重复 → 扣分（概率降低）
if st[i] in prev:
    icount -= 3
if st[i] in st[:i]:  # 句内重复
    icount -= 3

# 4. 句尾字与前文重复 → 拒绝
if st[-1]+'。' in prev or st[-1]+'，' in prev:
    return -1000
```

### 4.4 平仄规则 —— "二四六分明"

这是唐诗平仄的核心规则。七言句中，第 2、4、6 字的平仄必须严格遵守交替规律。

```python
# poem_verifier.py:334-386

# 检查第 2 字（index 1）
pz1 = shengdict[st[1]]
if rhy != 2:                    # 平仄模式已确定
    if len(pz1) == 1:           # 单音字
        if rhy != pz1[0]:       # 平仄与模式不符 → 拒绝
            return -1000

# 检查第 4 字（index 3）—— 必须与第 2 字平仄相反
pz2 = shengdict[st[3]]
if rhy != 2:
    if len(pz2) == 1:
        if pz2[0] + rhy != 1:   # pz2 + rhy 必须等于 1（一平一仄）
            return -1000

# 检查第 6 字（index 5）—— 必须与第 2 字平仄相同（仅七言）
if len(st) >= 6:
    pz3 = shengdict[st[5]]
    if rhy != 2:
        if len(pz3) == 1:
            if rhy != pz3[0]:
                return -1000
```

**平仄交替规律总结**：

| 位置 | 五言 | 七言 | 规则 |
|------|------|------|------|
| 第 2 字 | index=1 | index=1 | = rhy（奠定基调） |
| 第 4 字 | index=3 | index=3 | = 1-rhy（与第2字相反） |
| 第 6 字 | N/A | index=5 | = rhy（与第2字相同） |

### 4.5 孤平检测

孤平是唐诗格律中的大忌。系统在以下条件下检测孤平：

```python
# poem_verifier.py:388-407

if endrhy == 0:  # 平收句（押韵句）
    if rhy == 0:  # 平起句（如"平平仄仄平"）
        # 检查第1字(index=0)和第3字(index=2)
        # 如果第1字和第3字都是仄声 → 第2字变成孤平
        if len(st) >= 3:
            pz1 = shengdict[st[0]]
            pz3 = shengdict[st[2]]
            if len(pz1) + len(pz3) == 2:      # 都是单音字
                if pz1[0] + pz3[0] == 2:      # 两仄（1+1=2）
                    return -1000               # 孤平！

    if rhy == 1:  # 仄起句（如"仄仄平平仄"）
        # 检查第3字(index=2)和第5字(index=4)
        # 如果第3字和第5字都是仄声 → 第4字变成孤平
        if len(st) >= 5:
            pz1 = shengdict[st[2]]
            pz3 = shengdict[st[4]]
            if len(pz1) + len(pz3) == 2:
                if pz1[0] + pz3[0] == 2:      # 两仄
                    return -1000               # 孤平！
```

**孤平的通俗解释**：
- 在平收句（押韵句）中，如果韵脚前只有一个平声字被两个仄声字夹在中间，这个平声字就是"孤平"
- 例如"仄平仄仄平"中的"平"就是孤平（第 2 字位置）

### 4.6 句尾三连同

当句子完成时（`fullst == True`），检查句尾三连同：

```python
# poem_verifier.py:414-425

pz11 = shengdict[st[-3]]  # 倒数第 3 字
pz12 = shengdict[st[-2]]  # 倒数第 2 字
pz13 = shengdict[st[-1]]  # 倒数第 1 字（末字）

if len(pz11) + len(pz12) + len(pz13) == 3:  # 都是单音字
    if pz11[0] + pz12[0] + pz13[0] == 0:    # 三连平
        return -1000
    if pz11[0] + pz12[0] + pz13[0] == 3:    # 三连仄
        return -1000
```

**三连同在唐诗中是严重违规**：
- **三连平**：句尾连续三个平声字（平平平）
- **三连仄**：句尾连续三个仄声字（仄仄仄）

### 4.7 句尾提前三连同检测

在句子尚未完成但接近完成时（`len(st) > max_length - 3`），也做三连同的预检测：

```python
# poem_verifier.py:363-376

if len(st) > max_length - 3:  # 五言：超过2字；七言：超过4字
    if endrhy != 2:
        wrhy = rhy
        if max_length == 5:
            wrhy = 1 - rhy  # 五言末字平仄与基调相反
        if endrhy == wrhy:
            pz = shengdict[st[max_length - 3]]  # 倒数第3字
            if len(pz) == 1:
                if pz[0] == wrhy:
                    return -1000  # 将形成三连同
```

五言和七言此处有微妙差别：五言的末字（index=4）与 rhy 相反，七言的末字（index=6）与 rhy 相同。

### 4.8 押韵一致性

基于平水韵（`pingshui.txt`）进行韵部检测：

```python
# poem_verifier.py:438-455

if len(yayun) > 0 and endrhy == 0:  # 已有押韵字且当前是押韵句
    final1 = wdic[st[-1]]           # 当前末字的韵部列表
    final2 = []
    for i in yayun:
        final2.append(wdic[i])      # 已积累的所有押韵字的韵部列表

    doc = 0
    for i in final1:
        doc = 1
        for td in final2:
            if not (i in td):       # 韵部不匹配
                doc = 0
        if doc == 1:
            break
    if doc == 0:
        return -1000  # 押韵不一致！
```

押韵一致性的核心逻辑：
1. 取当前句末字的韵部
2. 与所有已积累的押韵句末字韵部逐一比较
3. **必须存在至少一个共同的韵部**（多音字放行的体现）
4. 否则视为出韵 → 拒绝

### 4.9 句尾平仄收束

```python
# poem_verifier.py:427-436

if endrhy != 2:                     # 平仄收束模式已确定
    if len(pz13) == 1:              # 单音字
        if endrhy != pz13[0]:       # 末字平仄与要求不符
            return -1000            # 例如押韵句必须以平声收尾

if endrhy == 2:                     # 首句，尚未确定
    if len(pz13) == 1:
        endrhy = pz13[0]            # 用首句末字确定收束模式
```

### 4.10 首句末字特殊限制

```python
# poem_verifier.py:412-413

if st[-1] in ['不']:
    return -1000  # 末字不能是"不"
```

---

## 五、生成流程

### 5.1 两套流程对比

| 特性 | `bipro_poem.py`（MultiGen） | `inference_poems_singlegen.py`（SingleGen） |
|------|---------------------------|------------------------------------------|
| 策略类 | `MultiGenStrategy` | `SingleGenStrategy` |
| beam 数 | 6 | 1 |
| 迭代优化 | 有（refine + inverse prompting） | 无 |
| IP 评分排序 | 有 | 无 |
| 4句/8句比较 | 有 | 无 |
| 速度 | 慢 | 快 |

### 5.2 逐句生成流程

```
输入模板: 诗歌《标题》 作者:李白 体裁:格律诗 情感:xxx 标题:xxx 正文:[gMASK]

for ids in range(total_lines):        # 4句或8句
    1. 设置当前句的参数:
       - 结束标点: 偶数句用"，" 奇数句用"。"
       - 押韵控制: 偶数句传空，奇数句传 yayun
       - 收束模式(endrhy): 偶数句=1(仄收), 奇数句=0(平收)
       - 平仄模式(rhy): 按句号决定 rhy 或 1-rhy

    2. 自回归生成:
       while 未完成:
           model.forward() → logits
           strategy.forward(logits, tokens, mems)
             → 对每个候选 token:
                 beam = tokens + new_token
                 verify_score = poem_verifier(beam, params)
                 if verify_score < -100: 跳过该候选
                 if verify_score > 100: 句子完成，加入 end_beams
           → 保留通过校验的 beam 继续

    3. verify_rhy() 更新状态:
       - 确定押韵字 yayun
       - 确定全诗平仄基调 rhy
       - 确定字数 length

    4. (MultiGen only) 逆提示精炼:
       用 [sMASK] 替换刚生成的句子
       反向生成新候选
       比较新旧版本，择优保留

    5. 构造下一句输入: prev + new_sentence + [gMASK]
```

### 5.3 inverse prompting 评分流程（MultiGen 专用）

```python
# ip_score.py

def compute_ip(model, answer, tokenizer):
    # 1. 提取标题部分，反向预测
    inv_title = "... 标题：[sMASK] 正文..."
    target_title = "原标题"
    ppl_title = ppl(model, inv_title, target_title, ...)

    # 2. 提取情感部分，反向预测
    inv_emo = "... 情感：[sMASK] 标题..."
    target_emo = "原情感"
    ppl_emo = ppl(model, inv_emo, target_emo, ...)

    # 3. 提取相邻句，反向预测
    inv_prev, target_prev = get_correspond_sentence(answer)
    ppl_prev = ppl(model, inv_prev, target_prev, ...)

    # 4. 罚分：标题字出现在正文中 → 扣分
    ppl_title -= nn * nn

    return ppl_title, ppl_emo, ppl_prev

def aggregate_scores(score_title, score_emo, score_cor):
    return score_title + 0.2 * score_emo + 0.15 * score_cor
```

逆提示评分的核心思想：**给定生成的诗句，反过来预测标题、情感和上下文——越容易预测回来，说明诗句与主题越一致，质量越高。困惑度越低 → 得分越高。**

### 5.4 迭代精炼流程（MultiGen 专用）

```python
# bipro_poem.py:361-427

for ids in range(total_lines):
    # 1. 拆分当前诗句
    bf, af, sp = splitpoem(ids, poem)

    # 2. 构造 [sMASK] 输入
    new_sentence = pretext + bf + '[sMASK]' + af

    # 3. 重新生成该句
    out = generate_sentence_with_constraint(...)

    # 4. 替换
    poem_full = new_sentence.replace('[sMASK]', out)

    # 5. 更新押韵状态
    yayun, rhy, length = verify_rhy(out, ids, ...)

# 全诗完成后：
for i in range(refine_times):
    poem, q = refine_poem(...)
    if q == 0:  # 没有变化 → 收敛
        break
```

---

## 六、verify_rhy —— 押韵状态追踪

```python
# poem_verifier.py:463-506

def verify_rhy(sentence, ids, shengdict, yayun, rhy):
    # 1. 提取当前句
    st = get_last_piece(sentence)[:-1]  # 去掉标点

    # 2. 判断是否需要押韵
    needyy = 0
    if ids == 0:
        if 末字是平声:
            needyy = 1  # 首句平收 → 入韵
    if ids % 2 == 1:
        needyy = 1      # 奇数句必须押韵

    if needyy == 1:
        yayun += st[-1]  # 记录韵脚

    # 3. 确定全诗平仄基调（rhy）
    if rhy != 2:  # 已确定
        return yayun, rhy, length

    # 从当前句的第2字、第4字或第6字确定 rhy
    # ids 0,3,4,7: 直接使用检测到的平仄
    # ids 1,2,5,6: 使用反向平仄
    pz1 = shengdict[st[1]]       # 第2字
    if len(pz1) == 1:
        rhy = pz1[0]
        if ids in [1,2,5,6]:
            rhy = 1 - rhy
        return ...

    pz2 = shengdict[st[3]]       # 第4字
    if len(pz2) == 1:
        rhy = 1 - pz2[0]
        if ids in [1,2,5,6]:
            rhy = 1 - rhy
        return ...

    if len(st) > 5:
        pz3 = shengdict[st[5]]   # 第6字
        if len(pz3) == 1:
            rhy = pz3[0]
            if ids in [1,2,5,6]:
                rhy = 1 - rhy
```

**平仄基调确定优先级**：第 2 字 > 第 4 字 > 第 6 字（多音字时跳过）

---

## 七、策略类 —— 受限采样机制

### 7.1 共同接口

所有策略类都实现：
- `forward(logits, tokens, mems)` → 采样 token 并调用 verifier 过滤
- `finalize()` → 返回最终 beam 结果

### 7.2 MultiGenStrategy（多样本采样）

```python
# mtgen.py

class MultiGenStrategy:
    def forward(self, logits, tokens, mems, fixed_next=None):
        # 1. 计算采样分数
        scores_for_sample = logits * tmp_factor

        # 2. 多轮采样循环
        while total_added < num_beams:
            probs = softmax(scores_for_sample)
            next_tokens = multinomial(probs, num_samples * 2)

            for token in next_tokens:
                beam = tokens[i] + token

                # ★ 关键：每个候选都要经过 verifier
                verify_score = self.verifier(beam, self.verifier_params)
                if verify_score < -100:
                    continue  # 格律违规 → 丢弃

                if token in end_tokens or verify_score > 100:
                    self._add_end_beams(beam)  # 句子完成
                else:
                    beam_continue.append(beam)  # 继续生成
```

### 7.3 BeamSearchStrategy（束搜索）

与 `MultiGenStrategy` 结构相似，但多了：
- 累积分数（`cached_beam_scores`）：综合历史得分
- n-gram 禁止重复（`no_repeat_ngram_size`）
- 更多提前终止条件

### 7.4 iPromptStrategy（逆提示增强策略）

在 BeamSearchStrategy 基础上增加：
- 每一步都计算 inverse prompting score
- beam 重排序：综合模型概率 + IP 评分 + 校验得分
- 支持 `ban_end` 模式（强制继续生成）

---

## 八、校验返回值体系

| 返回值 | 含义 |
|--------|------|
| `-1000` | **严重违规**，直接丢弃该候选 |
| `0` | **正常**，句子未完成，可以继续 |
| `1000` | **句子完整且通过所有校验**，加入 end_beams |
| `icount` | **扣分累加**，< 0 表示有小问题但可容忍 |

---

## 九、关键设计决策

### 9.1 为什么用 verifier 而不是规则生成器？

传统做法是先规定平仄模板，再填字。但这样做：
- 无法利用大模型的语义理解能力
- 生成的诗句往往生硬不自然

本系统采用**"先生成、后过滤"**的策略：
- 让模型自由生成候选字
- 用规则实时过滤不合规的候选
- 保留模型的语言流畅性

### 9.2 多音字妥协

```python
if len(shengdict[char]) > 1:
    # 多音字，直接放行，不做平仄限制
    pass
```

这是一个工程上的务实选择：
- 多音字在不同语境下读不同的声调
- 强制单一声调会错误地拒绝好诗
- 宁愿放宽约束，交由后续的人工/自动评估

### 9.3 首句定基调

```python
if rhy == 2:  # 未确定
    pz1 = shengdict[st[1]]
    if len(pz1) == 1:
        rhy = pz1[0]  # 用首句第 2 字确定全诗平仄基调
```

全诗的平仄模式由第一句的第 2 字一锤定音。

### 9.4 偶数句不押韵的"宽松"

```python
if ids % 2 == 0:
    strategy.set_verifier_param('', 8)   # 偶数句传空 → 不检查押韵
else:
    strategy.set_verifier_param(yayun, 8) # 奇数句传韵脚 → 必须押韵
```

### 9.5 标点交替

```python
def reverse_end(endtk):
    if '。' in endtk:
        new_endtk = [',', '，']  # 下一句用逗号结尾
    else:
        new_endtk = ['.', '。']  # 下一句用句号结尾
```

生成过程中标点在逗号和句号之间交替，模拟真实的诗歌标点结构。

---

## 十、总结

本系统的唐诗格律实现覆盖了传统格律诗的所有核心规则：

| 规则类别 | 具体规则 | 实现位置 |
|----------|---------|---------|
| **字数** | 五言 5 字、七言 7 字，禁止 6 字 | poem_verifier.py:268-284 |
| **禁字** | 禁止'的''些''么''了'等现代虚词 | poem_verifier.py:317-320 |
| **重字** | 禁止 2-3 字连续重复，单字重复扣分 | poem_verifier.py:254-316 |
| **平仄** | 二四六分明，第 2/4/6 字严格卡位 | poem_verifier.py:334-386 |
| **孤平** | 平收句中检测孤平 | poem_verifier.py:388-407 |
| **三连同** | 禁止三连平/三连仄 | poem_verifier.py:414-425 |
| **押韵** | 一韵到底，偶数句押韵 | poem_verifier.py:438-455 |
| **句尾收束** | 偶数句仄收、奇数句平收 | poem_verifier.py:427-436 |
| **首句入韵** | 首句平收则入韵 | verify_rhy:472-475 |
| **平仄交替** | 第 0/3/4/7 句同调，第 1/2/5/6 句反弹 | verify_rhy:490-505 |
| **多音字** | 多音字直接放行 | 多处 len(pz)==1 判断 |

**整体架构**：GLM-10B 模型提供语言能力 → 受限采样策略控制生成过程 → poem_verifier 实时过滤确保格律合规 → inverse prompting 评分优选 → 迭代精炼优化质量。
